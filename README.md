# CLA signatures

Acceptances recorded by `.github/workflows/cla.yml`. This branch is metadata,
not source, and is kept off `main` so a signature does not trigger a full
desktop build. Do not merge it.
